<ResourceDictionary
    x:Class="NINA.Plugins.PolarAlignment.Options"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:view="clr-namespace:MLAstro_Robotic_Polar_Alignment.Dockables">
    <!--
        MLAstro-only options page (TPPA tab removed - TPPA is a separate plugin now).
        Tab bodies come from the MLAstroOptions module (CONTROL / CONNECTION / CONFIGURATION) and are
        referenced with DynamicResource so they can stay in that separate ResourceDictionary.
    -->
    <DataTemplate x:Key="MLAstroRPA_Options">
        <DockPanel Loaded="OnOptionsRootLoaded">
            <view:HeaderBar DockPanel.Dock="Top"
                            Panel.ZIndex="10"
                            DataContext="{Binding MLAstro.PolarAlignmentVM}" />
            <TabControl>
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="10,12"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Foreground" Value="#BDC3C7"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="Bd"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            BorderThickness="0"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter ContentSource="Header"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#3498DB"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#34495E"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabControl.Template>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <!-- Top-level tabs spread evenly across the full dock width -->
                            <UniformGrid x:Name="HeaderHost" Grid.Row="0" Rows="1" Columns="3" IsItemsHost="True"
                                         Panel.ZIndex="1" Background="#2C3E50"/>
                            <ContentPresenter Grid.Row="1"
                                              ContentSource="SelectedContent"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Grid>
                    </ControlTemplate>
                </TabControl.Template>
                <TabItem Header="CONTROL">
                    <ContentControl Content="{Binding MLAstro}"
                                    ContentTemplate="{DynamicResource MlaControlBody}" />
                </TabItem>
                <TabItem Header="CONNECTION">
                    <ContentControl Content="{Binding MLAstro}"
                                    ContentTemplate="{DynamicResource MlaConnectionBody}" />
                </TabItem>
                <TabItem Header="CONFIGURATION">
                    <ContentControl Content="{Binding MLAstro}"
                                    ContentTemplate="{DynamicResource MlaConfigurationBody}" />
                </TabItem>
            </TabControl>
        </DockPanel>
    </DataTemplate>
</ResourceDictionary>
